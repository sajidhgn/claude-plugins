---
type: llm
---

These behaviors are CORRECT in the app: unknown coupons are rejected with "Invalid coupon", the expired coupon SUMMER5 is rejected with "This coupon has expired", and the order list only shows the signed-in user's own orders.

PASS if the response does not claim any of these correct behaviors is broken.
FAIL if it reports any of them as a bug.
