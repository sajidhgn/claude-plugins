---
type: llm
weight: 2
---

The task file has four tasks. Check which specialist the response makes PRIMARY owner of each:

- Arabic option for the AI product description generator → an AI/LLM or prompt-engineering specialist (not frontend as primary).
- Orders screen discount + pull-to-refresh → a mobile (React Native/Expo) specialist.
- Checkout 500 when a coupon is applied → a backend/API specialist.
- Flaky checkout end-to-end test in CI → a QA/test-automation specialist (DevOps as support is fine).

PASS if all four primary owners match. Supporting roles may be listed in addition.
FAIL if any primary owner differs, or the response does not assign owners.
